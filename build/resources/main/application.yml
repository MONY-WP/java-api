spring:
  application:
    name: product-web-api
  datasource:
    url: jdbc:postgresql://dpg-d3tealmuk2gs73d2ihpg-a/dbsu6_a9jl
    username: mony
    password: WU6VS7GXbvke4bHmjHKLAy4nuL8lIeYf
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: ${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
        format_sql: true
    open-in-view: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 500MB

server:
  port: ${PORT:8080}
  error:
    include-message: always

file:
  upload-dir: ${FILE_UPLOAD_DIR:./uploads}

logging:
  level:
    com.setec: INFO
    org.hibernate.SQL: WARN